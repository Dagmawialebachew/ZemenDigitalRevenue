import type { Order } from '../api/types'
import { Empty, SectionHead, Status, dt, money } from '../components/UI'
export function OrdersView({items}:{items:Order[]}){return <div className="page-stack"><SectionHead title="Orders" subtitle="Every checkout attempt, whether it paid or stopped."/>{!items.length?<Empty/>:<div className="table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Product</th><th>Price</th><th>Source</th><th>Status</th><th>Created</th></tr></thead><tbody>{items.map(o=><tr key={o.id}><td><b>{o.public_id}</b><small>{o.pricing_type==='regular'?'Full price':'Discount'}</small></td><td><b>{o.first_name}</b><small>{o.username?`@${o.username}`:o.telegram_id}</small></td><td>{o.product_title}</td><td><b>{money(o.total_due_br)}</b>{Number(o.discount_total_br)>0&&<small>−{money(o.discount_total_br)}</small>}</td><td>{o.creative||o.campaign||o.platform||'Direct'}</td><td><Status value={o.status}/></td><td>{dt(o.created_at)}</td></tr>)}</tbody></table></div>}</div>}
